# Production Configuration
server.port=${PORT:8080}
server.address=0.0.0.0

# Database Configuration (NeonDB)
spring.datasource.url=${DATABASE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Security
spring.security.user.name=admin
spring.security.user.password=${ADMIN_PASSWORD:admin}

# JWT Configuration
jwt.secret=${JWT_SECRET:your-256-bit-secret-key-change-this-in-production-min-32-chars}
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=${FRONTEND_URL:https://your-app.vercel.app}

# Logging
logging.level.root=INFO
logging.level.com.team.chatapp=INFO

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# WebSocket Configuration
websocket.allowed.origins=${FRONTEND_URL:https://your-app.vercel.app}
